const Loading = () => {
  return (
    <div className={"w-full flex justify-center h-full items-center"}>
      <div className="animate-spin border-2 w-10 h-10 border-b-transparent border-foreground rounded-full"></div>
    </div>
  );
};

export default Loading;
